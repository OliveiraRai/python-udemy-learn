Aprendizado sobre:

- Loops
- Loops dentro de loops
- Módulo isalpha() para validação de strings
- Módulo lower() que transforma uppercase em lowercase
- Módulo append() que insere novos valores numa lista
- "if x in y" É algo que eu não pensaria sozinho, mas agora vejo que existem mais opções do que imaginei
    - No código que usa essa linha, é a linha da lógica de Game Over, onde se guess não estiver em word, subtrai de lives; se lives for igual a 0, é Game Over.
- Uso do operador lógico NOT
- Uso de lógica para descobrir quantas letras faltam para serem descobertas e usar a mesma variável para a lógica de vencer o jogo
